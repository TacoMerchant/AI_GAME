///tmc_ll_item_equals(item1, item2);
/*
    Checks it the 2 items are the same item, using their UIDs
    returns true if same item, false if not
*/

return(tmc_ll_item_get_id(argument0) == tmc_ll_item_get_id(argument1))
